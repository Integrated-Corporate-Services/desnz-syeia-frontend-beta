/**
 * Session Timeout Configuration
 */

// Session timeout duration in seconds (30 minutes)
export const SESSION_TIMEOUT = 30 * 60;

// Warning modal display time in seconds (2 minutes before timeout)
export const SESSION_WARNING = 2 * 60;

// Redirect path after session timeout
export const SIGNED_OUT_PAGE = '/frontend/signed-out';
